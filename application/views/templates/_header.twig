        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
        <title>
            {% set _page_title = [function('config_item','app_name')] %}
            {% if page_title %}
                {% set _page_title = [page_title]|merge(_page_title) %}
            {% endif %}
            {{ _page_title|join(' | ') }}
        </title>
        <link rel="apple-touch-icon" sizes="180x180" href="{{ function('base_url', '/assets/icons/apple-touch-icon.png') }}">
        <link rel="icon" type="image/png" sizes="32x32" href="{{ function('base_url', '/assets/icons/favicon-32x32.png') }}">
        <link rel="icon" type="image/png" sizes="16x16" href="{{ function('base_url', '/assets/icons/favicon-16x16.png') }}">
        <link rel="manifest" href="{{ function('base_url', 'manifest.json') }}">
        <link rel="mask-icon" href="{{ function('base_url', '/assets/icons/safari-pinned-tab.svg') }}" color="#000000">
        <link rel="shortcut icon" href="{{ function('base_url', '/assets/icons/favicon.ico') }}">
        <meta name="msapplication-config" content="{{ function('base_url', 'browserconfig.xml') }}">
        <meta name="theme-color" content="#ffffff">
        
        <!-- Include all compiled plugins (below), or include individual files as needed -->
        <link href="{{ function('base_url','assets/includes/font-awesome/css/font-awesome.min.css') }}" rel="stylesheet">
        <link href="{{ function('base_url','assets/css/bootstrap.min.css') }}" rel="stylesheet">
        <link href="{{ function('base_url','assets/includes/bootstrap-datetimepicker/build/css/bootstrap-datetimepicker.min.css') }}" rel="stylesheet">
        <!-- Override all compiled plugins (below) -->
        <link href="{{ function('base_url','assets/css/style.min.css') }}" rel="stylesheet">

        <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
        <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
          <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
          <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
        <![endif]-->
